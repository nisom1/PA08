#include <stdbool.h>
#include <string.h>
#include <stdlib.h>

#ifndef EVAL 
//int expressionValid(char* filename);
// expressionRead reads the inputs file and verifies that the given input expression has
// the right operand : operator ratio

float calcValues(Stack * list, char ch);
int parseExpression (Stack list, char* filename); // RPN calculator
#endif 
